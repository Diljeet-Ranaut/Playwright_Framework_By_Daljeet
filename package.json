{
  "name": "saucelabs-playwright",
  "version": "1.0.0",
  "description": "Playwright automation for SauceLabs checkout flow",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "typescript": "^5.0.0",
    "allure-playwright": "^2.34.1"

  }
}
